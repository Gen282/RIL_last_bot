services:
  - type: web
    name: ril-cashier-bot
    runtime: python
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
